# 業務アプリ 共有・配布ガイド

ハゼモト建設の現場担当者にアプリを使ってもらうための手順をまとめます。

---

## 1. 共有用URLの取得

本アプリは現在Manus開発環境上で稼働しています。**URLは固定ではなく、Manusのセッションが更新されるたびに変わります**。担当者に共有する前に、社長（管理者）が**現時点で有効なURL**を確認してください。

### URLの確認方法

1. Manusコンソールにログイン
2. このプロジェクト（sns-automation-app）の画面で、現在稼働中のプレビューURLを確認
3. URLは以下のような形式です:

   ```
   https://<プロジェクトID>.manus-asia.computer
   ```

4. このURLを開いて、ログイン画面が表示されることを確認してから担当者に共有

### 担当者への共有テンプレート（Chatworkで送る例）

```
お疲れさまです。
建物点検アプリのURLを送ります。

[ここにURLを貼り付け]

スマホで開いて、ホーム画面に追加して使ってください。
手順は社内ガイド（SHARE.md）の「2. スマホへのインストール」を参照。
```

> ⚠️ **URLが変わったら**: Manus環境ではセッションが切れるとURLが変わります。担当者から「404」「ページが見つかりません」と連絡があったら、新しいURLを取得して再送してください。
> 
> 本格運用時は「**5. 本番ドメインへの移行**」を検討してください。これでURLが固定化されます。

---

## 2. スマホへのインストール（PWA）

現場でスマホから使うため、ホーム画面にアプリとして追加できるようPWA対応済みです。

### iPhone（Safari）
1. SafariでアプリのURLを開く
2. 画面下の **共有ボタン**（□に↑）をタップ
3. **ホーム画面に追加** をタップ
4. 名前を確認して **追加**
5. ホーム画面に「ハゼモト業務」アイコンが表示される

### Android（Chrome）
1. ChromeでアプリのURLを開く
2. 右上の **︙メニュー** をタップ
3. **アプリをインストール** または **ホーム画面に追加** をタップ
4. 確認して **インストール**
5. アプリ一覧／ホーム画面にアイコンが表示される

インストール後はネイティブアプリのように起動でき、アドレスバーが消えて操作領域が広くなります。

### ショートカット機能
インストール後、ホーム画面のアイコンを長押しすると、以下のショートカットが利用できます:
- **🏗️ 建物点検AI** → `/inspection`
- **📝 かんたん投稿** → `/simple-post`

---

## 3. 担当者の初回ログイン手順

1. アプリのURLを開く
2. **「ログイン」ボタン** をクリック
3. Manus OAuthポータルでログイン
4. 初回のみアプリへのアクセスを許可
5. ダッシュボードに遷移したら準備完了

> 💡 **新規ユーザー追加**: 担当者にもManusアカウントが必要です。社長アカウントから招待 or 各自で新規登録してもらってください。

---

## 4. 建物点検AIの現場での使い方

1. ホーム画面の「点検」ショートカット、または左メニュー **建物点検AI** から起動
2. **撮影情報を入力**
   - 団地（下到津 / 沼 / 日豊 / 向山）
   - 棟番号・階・方位（任意）
   - 点検対象部位（外壁 / 屋上 / 自動判定）
3. **その場で撮影** ボタンでスマホカメラ起動 → 撮影
4. AIが自動分析（数十秒で完了）
5. 結果を確認・必要なら削除や追加撮影
6. 「**DB保存**」で点検記録を保存
7. 「**PDF調書**」「**CSV**」で書類として出力

---

## 5. 本番ドメインへの移行（オプション）

開発用URLではなく、自社ドメイン（例: `inspection.hazemoto.co.jp`）で運用したい場合の選択肢:

### A. Manus環境のまま独自ドメインを割り当てる
- 一番ローコスト
- Manusサポートへ依頼が必要

### B. 自社サーバー / クラウドへ移行
本リポジトリは標準的なTypeScript製のフルスタックアプリのため、以下のような環境にデプロイ可能です:
- AWS（ECS / Lightsail / Amplify）
- Vercel（フロント）+ Railway/Render（API）
- 社内サーバー（Docker）

#### 必要な環境変数
```
DATABASE_URL=mysql://user:pass@host/db    # MySQL接続文字列
JWT_SECRET=ランダム文字列                  # セッションCookieの署名鍵
VITE_APP_ID=ManusアプリID                 # Manus OAuth用
VITE_APP_TITLE=ハゼモト業務               
VITE_OAUTH_PORTAL_URL=...                 # Manus OAuthポータルURL
BUILT_IN_FORGE_API_URL=https://forge.manus.im   # 画像保存・LLM呼び出し
BUILT_IN_FORGE_API_KEY=Forge発行キー
OAUTH_SERVER_URL=...                      # OAuthサーバー
OWNER_OPEN_ID=社長アカウントOpenID         # 管理権限ユーザー
NODE_ENV=production
PORT=3000
```

#### ビルド・起動
```bash
pnpm install
pnpm db:push      # 初回のみ。DBにテーブル作成
pnpm build        # 本番ビルド
pnpm start        # 本番サーバー起動（ポート3000）
```

#### 移行時の注意点
- **Manus Forge（画像保存・LLM）** に依存しているため、Manus環境外へ移行する場合は別途OpenAI/Gemini APIキーへの差し替えが必要です（`server/_core/llm.ts`、`server/storage.ts` の改修）
- **Manus OAuth** も同様に、社内認証（Google Workspaceなど）への差し替え検討が必要です

---

## 6. トラブルシューティング

| 症状 | 原因 | 対処 |
|---|---|---|
| ログイン画面から進めない | Manusアカウント未作成 | 各担当者に新規登録してもらう |
| カメラが起動しない | スマホブラウザのカメラ権限OFF | 設定 → サイトの権限 → カメラを許可 |
| 写真分析がずっと進まない | 通信が遅い／写真が大きすぎる | Wi-Fi接続 or 写真サイズを縮小（10MB以下） |
| PDFが文字化けする | 古いブラウザ | Chrome / Safari の最新版に更新 |
| 「DBに接続できません」エラー | `DATABASE_URL`未設定 | 環境変数を確認 |
| ホーム画面に追加できない | プライベートブラウジング中 | 通常モードで開き直す |

---

## 7. 機能一覧

| 機能 | URL | 用途 |
|---|---|---|
| 建物点検AI | `/inspection` | 福岡県住宅供給公社の屋上・外壁定期点検 |
| かんたん投稿 | `/simple-post` | 1枚の写真からSNS投稿文生成 |
| AI投稿生成 | `/demo` | 5枚カルーセル投稿 |
| 投稿カレンダー | `/calendar` | 予約投稿の管理 |
| 効果測定 | `/analytics` | 投稿のパフォーマンス分析 |
| 設定 | `/settings` | アカウント・通知設定 |

---

最終更新: 2026-06-24
